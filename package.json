{
  "name": "infinite-formula-dungeon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "test": "node --test",
    "check": "node --check src/*.js && node --check tests/*.js"
  }
}
