# Testing

## Testing Strategy

seed固定可能な純粋ロジックをNode.js標準test runnerで検証します。UIはブラウザで主要ブレークポイントと操作を手動確認します。

## Validation Matrix

| 変更タイプ | 必要な検証 |
| --- | --- |
| 式・敵・状態遷移 | `npm test`、`npm run check` |
| UI/CSS | 上記に加えブラウザ操作とレスポンシブ表示 |
| イベント | seed再現性、15%抽選、5種の選択結果、報酬互換、ラン補正とv3保存 |
| 階層変異 | 区間切替、seed再現性、非連続、6効果の適用範囲、イベント・特性との分離、v3保存 |
| ビルド分岐 | 節目と上限、seed候補、5効果、特性・変異との合成、イベント分離、v3保存 |
| 文書のみ | 文書リンクと実装との整合 |

## Fast / Full Validation

- Fast: `npm run check`
- Full: `npm test && npm run check`
- Unit: `node --test`
- Build: 不要（native ES Modules）

## Covered rules

seed再現性、深層スケール、格差/OBS/観察による開示、防御、EXP・Gold格差、強敵率と上限、Threat増減、強敵能力・報酬、死亡、予兆一致、レベルアップ、購入、系列アンロック、ドロップ個体差、装備再計算、鍛錬減衰、進行、v2/v3保存復元を自動検証します。

敵図鑑は初回・再遭遇、初回/最深階層、勝利時だけの撃破加算、逃走、未遭遇情報の秘匿、v3保存復元と端末内メタ統合を自動検証します。

ラン記録は撃破・強敵・獲得Gold・装備・最大与ダメージの集計、新規ラン時の一時統計だけの初期化、ゲームオーバー時の確定、新記録判定、ベスト非更新、履歴10件制限、再読込と旧保存補完を自動検証します。

装備特性は特性有無とseed再現性、単一付与、定義不変、6効果の条件、重複防止、装備解除、ショップ除外、v3保存復元と旧装備補完を自動検証します。

専門強化はLevel節目、候補再現性、重複取得と3回上限、5効果の適用条件、装備特性・階層変異との合成、イベント報酬の分離、新規ラン初期化、v3保存復元と旧保存補完を自動検証します。

キーボード層は状態別割当、大文字小文字、予約キー非使用、`event.repeat`、入力ロック、編集要素、Cmd/Ctrl/Alt除外を自動検証します。

## Manual Verification

スマートフォンは375×667、390×844、393×852、430×932で縦横スクロール、重なり、画面外ボタン、敵HP・予兆・探索者HP・固定下部操作領域の全行動・メニューの同時可視性を確認します。戦闘開始から行動、撃破、次の敵、階層移動、再戦闘まで進め、固定下部操作領域が同じ位置へ復帰することと、通常敵・強敵や情報開示による大きなレイアウトシフトがないことを確認します。戦闘ログはスマホメニューから開けること、スキルメニューが固定下部操作領域の上に収まることも確認します。補助モーダルは画面内スクロールを確認し、PCは768px・1280pxで既存構成とキーボード操作を確認します。全行動、スキルメニュー、敵図鑑、勝利後の準備、ショップ、所持品、装備と特性表示、保存・続行、ゲームオーバーと再挑戦を操作します。
